name: Update Submodules

on:
  workflow_dispatch:

jobs:
  update-submodules:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Set up Git
        run: |
           git config --global user.email "actions@github.com"
           git config --global user.name "GitHub Actions"

      - name: Update YTMusicUltimateDX submodules
        run: |
          git submodule remove https://github.com/PoomSmart/YouTubeHeader.git Tweaks/YouTubeHeader
          git submodule update --init --recursive --remote https://github.com/PoomSmart/YouTubeMusicHeader.git Tweaks/YouTubeMusicHeader
          git submodule update --init --recursive --remote https://github.com/PoomSmart/YTMABConfig.git Tweaks/YTMABConfig
          git submodule update --init --recursive --remote https://github.com/PoomSmart/Return-YouTube-Dislikes.git Tweaks/Return-YouTube-Dislikes
          git submodule update --init --recursive --remote https://github.com/PoomSmart/Return-YouTube-Music-Dislikes.git Tweaks/Return-YouTube-Music-Dislikes
          git add .
          git commit -m "Updated YTMusicUltimateDX submodules"
          git push origin
        continue-on-error: true

      - name: Check exit code
        run: |
          exit_code=$?
          if [ $exit_code -ne 0 ]; then
            echo "Submodule update failed with exit code $exit_code"
            exit 1
          fi
